name: Selected days header verification

on:
  push:
    branches:
      - CHATGPT_TEST
    paths:
      - .github/workflows/chatgpt-test-selected-days-header.yml

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: CHATGPT_TEST
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Apply selected days header layout
        run: |
          python3 <<'PY'
          from pathlib import Path

          def replace_once(path, old, new, label):
              p = Path(path)
              text = p.read_text(encoding='utf-8')
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{label}: expected 1 match, got {count}')
              p.write_text(text.replace(old, new, 1), encoding='utf-8')

          replace_once(
              'admin/index.html',
              '''                        <div class="admin-naptar-lista-fej">\n                            <button type="button" id="admin-naptar-kijeloles-torles" class="admin-kis-gomb admin-veszely-gomb">Kijelölés törlése</button>\n                        </div>\n                        <div id="admin-naptar-kijelolt-lista" class="admin-naptar-kijelolt-lista" aria-label="Kijelölt napok"></div>''',
              '''                        <div class="admin-naptar-lista-fej">\n                            <h3>Kijelölt napok</h3>\n                            <button type="button" id="admin-naptar-kijeloles-torles" class="admin-kis-gomb admin-veszely-gomb">Kijelölés törlése</button>\n                        </div>\n                        <div id="admin-naptar-kijelolt-lista" class="admin-naptar-kijelolt-lista" aria-label="Kijelölt napok"></div>''',
              'selected days fixed header'
          )

          replace_once(
              'src/admin/00-bootstrap-auth-calendar.js',
              '''        datumok.forEach((datum, index) => {''',
              '''        datumok.forEach(datum => {''',
              'selected day iterator'
          )

          replace_once(
              'src/admin/00-bootstrap-auth-calendar.js',
              '''                <div class="admin-naptar-datum">\n                    ${index === 0 ? '<span class="admin-naptar-lista-cim">Kijelölt napok</span>' : ''}\n                    <span class="admin-naptar-datum-ertek">${html(datumRovid(datum))}</span>\n                </div>''',
              '''                <div class="admin-naptar-datum">${html(datumRovid(datum))}</div>''',
              'selected day date only'
          )

          replace_once(
              'src/admin-styles/70-availability.css',
              '''.admin-body.admin-v2 .admin-naptar-datum {\n  display: flex;\n  min-width: 0;\n  align-items: center;\n  gap: 8px;\n  color: var(--barna);\n  font-size: var(--lumi-font-label);\n  font-weight: 600;\n  line-height: 1.25;\n}\n.admin-body.admin-v2 .admin-naptar-lista-cim {\n  color: var(--tinta);\n  font-size: 14px;\n  font-weight: 600;\n  white-space: nowrap;\n}\n.admin-body.admin-v2 .admin-naptar-datum-ertek { white-space: nowrap; }''',
              '''.admin-body.admin-v2 .admin-naptar-datum {\n  min-width: 0;\n  color: var(--barna);\n  font-size: var(--lumi-font-label);\n  font-weight: 600;\n  line-height: 1.25;\n  white-space: nowrap;\n}''',
              'selected date ownership'
          )

          replace_once(
              'src/admin-styles/70-availability.css',
              '''.admin-body.admin-v2 #admin-idosav-naptar .admin-naptar-lista-fej {\n  grid-area: selected-title;\n  align-items: center;\n  justify-content: flex-end;\n  margin: 0;\n  padding-top: 3px;\n}''',
              '''.admin-body.admin-v2 #admin-idosav-naptar .admin-naptar-lista-fej {\n  grid-area: selected-title;\n  align-items: center;\n  justify-content: space-between;\n  margin: 0;\n  padding-top: 3px;\n}\n.admin-body.admin-v2 #admin-idosav-naptar .admin-naptar-lista-fej h3 {\n  margin: 0;\n  color: var(--tinta);\n  font-size: 18px;\n  font-weight: 600;\n}''',
              'selected header alignment'
          )

          replace_once(
              'src/admin-styles/70-availability.css',
              '''  .admin-body.admin-v2 #admin-idosav-naptar .admin-naptar-sor > .admin-naptar-datum {\n    grid-area: date;\n    align-self: center;\n    gap: 6px;\n    font-size: 12px;\n  }\n  .admin-body.admin-v2 #admin-idosav-naptar .admin-naptar-lista-cim { font-size: 12px; }''',
              '''  .admin-body.admin-v2 #admin-idosav-naptar .admin-naptar-sor > .admin-naptar-datum {\n    grid-area: date;\n    align-self: center;\n    font-size: 12px;\n  }''',
              'selected mobile date cleanup'
          )

          Path('tests/admin-availability-selected-days-mobile.spec.js').write_text(r'''const { test, expect } = require('playwright/test');
          const fs = require('fs');
          const path = require('path');

          function source(relativePath) {
              return fs.readFileSync(path.join(__dirname, '..', relativePath), 'utf8');
          }

          test('selected days keep a fixed header and split start/end evenly on mobile', async ({ page }) => {
              const html = source('admin/index.html');
              const calendarJs = source('src/admin/00-bootstrap-auth-calendar.js');
              const availability = source('src/admin-styles/70-availability.css');
              const css = [
                  '00-foundation.css',
                  '10-components.css',
                  '70-availability.css'
              ].map(file => source(`src/admin-styles/${file}`)).join('\n');

              expect(html).toContain('<h3>Kijelölt napok</h3>');
              expect(calendarJs).toContain('datumok.forEach(datum => {');
              expect(calendarJs).not.toContain('admin-naptar-lista-cim');
              expect(availability).toContain('justify-content: space-between;');
              expect(availability).toContain('grid-template-columns: repeat(2, minmax(0, 1fr));');

              await page.setViewportSize({ width: 390, height: 844 });
              await page.setContent(`<!doctype html><html><head><style>${css}</style></head>
              <body class="admin-body admin-v2"><main id="admin-tartalom">
                <div style="container-type:inline-size;container-name:admin-workspace;width:390px">
                  <div id="admin-idosav-naptar" class="admin-naptar-blokk">
                    <div class="admin-naptar-lista-fej">
                      <h3>Kijelölt napok</h3>
                      <button type="button" id="admin-naptar-kijeloles-torles" class="admin-kis-gomb admin-veszely-gomb">Kijelölés törlése</button>
                    </div>
                    <div id="admin-naptar-kijelolt-lista" class="admin-naptar-kijelolt-lista" aria-label="Kijelölt napok">
                      <div class="admin-naptar-sor">
                        <div class="admin-naptar-datum">28/08/26</div>
                        <label class="admin-mezo">Kezdés<input data-start type="time" value="09:00"></label>
                        <label class="admin-mezo">Vége<input data-end type="time" value="18:00"></label>
                        <button type="button" class="admin-kis-gomb admin-veszely-gomb admin-naptar-torles-x" data-naptar-torles aria-label="Törlés">×</button>
                      </div>
                    </div>
                  </div>
                </div>
              </main></body></html>`);

              const metrics = await page.evaluate(() => {
                  const title = document.querySelector('.admin-naptar-lista-fej h3').getBoundingClientRect();
                  const clear = document.querySelector('#admin-naptar-kijeloles-torles').getBoundingClientRect();
                  const start = document.querySelector('[data-start]').getBoundingClientRect();
                  const end = document.querySelector('[data-end]').getBoundingClientRect();
                  const card = document.querySelector('.admin-naptar-sor').textContent;
                  return {
                      titleLeft: title.left,
                      clearLeft: clear.left,
                      startWidth: start.width,
                      endWidth: end.width,
                      endValue: document.querySelector('[data-end]').value,
                      cardContainsTitle: card.includes('Kijelölt napok')
                  };
              });

              expect(metrics.titleLeft).toBeLessThan(metrics.clearLeft);
              expect(metrics.cardContainsTitle).toBe(false);
              expect(Math.abs(metrics.startWidth - metrics.endWidth)).toBeLessThan(2);
              expect(metrics.endValue).toBe('18:00');
          });
          ''', encoding='utf-8')
          PY

      - name: Reject override hacks
        run: |
          python3 <<'PY'
          from pathlib import Path
          import re
          code = re.sub(r'/\*.*?\*/', '', Path('src/admin-styles/70-availability.css').read_text(encoding='utf-8'), flags=re.S)
          if '!important' in code:
              raise SystemExit('Actual !important declaration found in availability CSS')
          PY

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Version assets
        run: npm run assets:version

      - name: Check
        run: npm run check

      - name: Install Edge
        run: npx playwright install --with-deps msedge

      - name: Affected admin tests
        run: npx playwright test tests/admin-availability-selected-days-mobile.spec.js tests/admin-availability-mobile.spec.js tests/admin-shared-components.spec.js tests/admin-v2-regressions.spec.js

      - name: Commit verified changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add admin/index.html src/admin/00-bootstrap-auth-calendar.js src/admin-styles/70-availability.css tests/admin-availability-selected-days-mobile.spec.js admin-v2.css admin-supabase.js
          git add -u
          git commit -m "Move selected days title to fixed header"
          git push origin HEAD:CHATGPT_TEST
