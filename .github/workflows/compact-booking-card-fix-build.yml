name: Compact booking card fix build

on:
  push:
    branches:
      - compact-booking-card-fix

concurrency:
  group: compact-booking-card-fix-build
  cancel-in-progress: true

jobs:
  supersede:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseding obsolete booking-card verification run."
